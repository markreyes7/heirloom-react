const CollectionHead = ({text1, text2}) => {
    

    return (
        <div className="header-pos">
            <h2 className='body-h2-style'>{text1}</h2>
            <p className='body-p-style'>{text2}</p>
        </div>
    )
}

export default CollectionHead
